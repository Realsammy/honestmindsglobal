// useAuth.ts
// If useAuth is defined in AuthContext.ts, ensure it is exported there.
// Otherwise, update the import path to the correct module that exports useAuth.
// Example implementation of useAuth if it does not exist elsewhere
export { useAuth } from '../contexts/AuthContext';
